@{
    ViewData["Title"] = "Acceso denegado";
}
<div class="text-center py-5">
    <i class="bi bi-shield-lock display-1 text-danger"></i>
    <h3 class="mt-3">Acceso denegado</h3>
    <p class="text-muted">Tu cuenta no tiene permisos para ver esta sección.</p>
    <a asp-controller="Account" asp-action="Login" class="btn btn-dark">Volver al login</a>
</div>
